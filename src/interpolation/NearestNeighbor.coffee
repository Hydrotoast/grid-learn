class NearestNeighbor
  # Interpolates the values of the variables with respect to the data by
  # taking on the values of the closest data points to the given `width` and
  # `height` parameters.
  interpolate: (data, width, height) ->
